# virtual environment
# a virtual environment is atool to isolate specific python environment on single mavhine, allwoing you to work on multiple project
# with different dependencies and packages without conflicts.
# this can be especially useful when working on project that jave conflicting pacakge version or package that are not compatible with eacg other.

# to create a virtual environment in python, you can use the venv module that comes with python 

# here's an example of how to create a virtual environment and activate it:

# create a virtual environment
#  ==python -a venv myenv==
# copy the above code in your command prompt in the selected location and paste the command 
# in the same way copy the same commands 

# activate the virtual environment(windows)
# myenv\Scripts\activate.bat

# deactivate the virtual environment
# deactivate

# we can also install package and libraries in the folder byt redirecting the folder into the terminal
# 